# Demo Definition

Demo must show:
1) deterministic replay driving FPGA
2) live counters: events_in, buckets_out, drops (ideally 0 at target load)
3) feature outputs to host
4) correctness summary vs golden model
5) latency + throughput numbers
6) utilization + timing screenshot/table

